<?xml version="1.0" encoding="utf-8"?>
<!--
    Custom big-content-view for the daily check-in notification (see
    DailyReminderNotifier.postInteractive). Everything here is built with system-themed
    widgets/attrs (?android:attr/...) rather than app colors, since RemoteViews is inflated
    by the notification shade in whatever light/dark theme the system is in, not this app's
    own Compose theme.

    Three factor toggle buttons live in fixed, positional slots (factor_button_1/2/3) -
    RemoteViews needs static view IDs, so DailyReminderNotifier binds whichever 3 ranked
    factors are relevant into these slots by index and hides any that aren't in use, rather
    than generating one button per factor.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">

        <Button
            android:id="@+id/factor_button_1"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:maxLines="2"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="Poor sleep" />

        <Button
            android:id="@+id/factor_button_2"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp"
            android:maxLines="2"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="Major stress" />

        <Button
            android:id="@+id/factor_button_3"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:maxLines="2"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:visibility="gone"
            tools:text="Alcohol" />
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/nothing_notable_button"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginEnd="4dp"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:text="@string/notification_nothing_notable" />

        <Button
            android:id="@+id/save_button"
            style="?android:attr/buttonStyleSmall"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="4dp"
            android:textAllCaps="false"
            android:textSize="12sp"
            android:text="@string/notification_save" />
    </LinearLayout>

</LinearLayout>
